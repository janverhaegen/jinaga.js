(user: DWS.USER) {
    worker:DWS.WORKER [
        worker->user:DWS.USER = user
    ]
    username:DWS.USER.NAME [
        username->user:DWS.USER = user
        !E {
             prior:DWS.USER.NAME [
                prior->username:DWS.USER.NAME = username
             ]
        }
    ]

} 